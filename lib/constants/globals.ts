export const acmsTranslationSource = "ACMS";

export enum ETranslationSource {
   ACMS = "ACMS",
   INDEX = "INDEX",
   ERROR = "ERROR",
}

export const initialAcms = {
   grid: {},
   columns: [],
   pageTitle: "",
};
export enum EMode {
   update = "update",
   create = "create",
   view = "view",
   delete = "delete",
}
