/* eslint-disable no-template-curly-in-string */
const yup_en = {
  string: {
    max: 'Input length must not exceed ${max} characters',
  },
  number: {
    max: 'Value must be less than or equal to ${max}',
  },
};

export default yup_en;
